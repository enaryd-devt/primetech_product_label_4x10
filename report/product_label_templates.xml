<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Odoo's label renderer gives its table a physical width.  With some
         wkhtmltopdf/Windows combinations that width starts at the left report
         margin and overflows on the right.  Constraining the table to its page
         makes the two side spaces equal without changing barcode alignment. -->
    <template id="report_producttemplatelabel_center_4x10"
              inherit_id="product.report_producttemplatelabel">
        <xpath expr="//div[contains(@class, 'page')]" position="inside">
            <t t-if="layout_wizard and layout_wizard.print_format == '4x10xprice'">
                <style>
                    .page {
                        box-sizing: border-box;
                        padding-left: 4mm !important;
                        padding-right: 4mm !important;
                    }
                    .page &gt; table {
                        box-sizing: border-box;
                        margin-left: auto !important;
                        margin-right: auto !important;
                        table-layout: fixed;
                        width: 100% !important;
                    }
                </style>
            </t>
        </xpath>
    </template>
</odoo>
